import React from 'react';
import { Nav } from './NavBar';
import { Header } from './Header';

const index = () => {
  return (
    <section>
        <Nav />
        <Header />
    </section>

  )
}

export default index